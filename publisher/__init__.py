"""Xalq Insurance Digital OS Publisher - the auto-publish layer.

Closes the gap between content creation (/post, /clip, /edit-video) and actually
shipping it. Free-first cascade (Postiz self-hosted → manual handoff), driven by
claude-agents/.claude/capabilities.md. Entry point: publisher/run.py (or /publish).
"""
