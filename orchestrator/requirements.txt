# Xalq Insurance Digital OS orchestrator - Python dependencies
# Install: python -m venv venv; .\venv\Scripts\Activate.ps1; pip install -r requirements.txt

# Multi-agent crews
crewai>=0.80.0
crewai-tools>=0.17.0

# LLM provider bindings (4-way hybrid)
langchain-anthropic>=0.3.0
langchain-google-genai>=2.0.0
langchain-groq>=0.2.0
langchain-ollama>=0.2.0

# Graph-based routing
langgraph>=0.2.0

# Vector DB client (Qdrant RAG)
qdrant-client>=1.12.0

# Environment + utilities
python-dotenv>=1.0.0
